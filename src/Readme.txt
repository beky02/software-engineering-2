this application  is for MVC arthicture pattern therfor 
> the model part is represented by model.java class in the android,This Model component manages
the system data and associated operations on that data.
>the view is represented by activity_main.xml and it define,manage how data is represented to
user.
>Controller part also represented by  Controller.java class which manage user interaction and 
passes this interactions to view and model.means that it takes the data from user and passes
to model to make associated currency operation and give back to the controller and give back 
to the view 